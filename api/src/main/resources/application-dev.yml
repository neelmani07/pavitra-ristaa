spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/pavitra_ristaa}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

pavitra:
  security:
    jwt:
      secret: ${JWT_SECRET}

logging:
  level:
    com.pavitraristaa: INFO
